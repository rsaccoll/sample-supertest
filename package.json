{
  "name": "sample-supertest",
  "version": "0.0.1",
  "description": "Little sample to use supertest (with others) ",
  "dependencies": {
    "@hapi/joi": "^15.0.3",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "joi-assert": "0.0.3",
    "mochawesome": "^3.1.1",
    "supertest": "^3.4.2"
  },
  "devDependencies": {
    "faker": "^4.1.0",
    "mocha": "^10.2.0",
    "mocha-parallel-tests": "^2.1.0"
  },
  "scripts": {
    "test": "mocha-parallel-tests --timeout 48000 tests/*.spec.js --reporter mochawesome",
    "test-contract:get": "mocha --timeout 30000 tests/contract/*.spec.js --reporter mochawesome"
  },
  "author": "Ramses Almeida",
  "license": "MIT"
}
